# Frontend Internship Assessment
- The code is divided into `site.html`, `style.css` and `script.js` for ease of understanding.
- The final code is bundled into single file `index.html` which is hosted on `github pages`.
